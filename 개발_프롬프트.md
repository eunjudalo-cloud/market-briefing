# 단계별 개발 프롬프트

`PRD.md`(v0.2)를 기준 사양으로 하는 4단계 프롬프트. 순서대로 실행하고, **각 단계 산출물을 확인한 뒤** 다음 단계로 넘어간다. 각 단계 프롬프트 앞에 아래 "공통 컨텍스트"를 붙이거나 함께 참조시킨다.

---

## 공통 컨텍스트 (매 단계 공통)

```
- 이 저장소의 PRD.md를 기준 사양으로 삼는다. PRD와 충돌하는 결정은 하지 말고, 모호하면 먼저 질문한다.
- 스택: Python 3.11 / requests / feedparser / openai / python-dotenv / pydantic / jinja2
- OS: Windows. 파일은 UTF-8 고정, 콘솔 출력 인코딩(cp949) 문제에 대비한다.
- 비밀값은 .env에서만 로드한다. 로그·스냅샷·메일·예외 메시지·커밋에 키를 노출하지 않는다.
- 저작권: 뉴스는 제목·링크·매체·발행시각만 저장/출력한다. 본문·요약 전재 금지.
- 표현 규칙: 추측성 문구, 매수/매도/보유 추천, 목표가·밸류에이션 판단 금지.
- 타임존은 항상 KST(Asia/Seoul) 기준. 수집 창은 T-1 18:00 ~ T 07:00.
```

---

## 1단계 — 기본 뼈대 작성

목표: 외부 API 실제 호출은 아직 구현하지 않는다. **타입이 완성된 스텁**으로 파이프라인이 처음부터 끝까지 실행되어 브리핑 `.md`가 생성되게 한다.

```
역할: 시니어 파이썬 개발자.

PRD.md와 공통 컨텍스트를 따른다. 아래 뼈대를 만들어라. 수집기/분석기는 스텁(고정 mock 데이터를 반환)으로 두되, 모델·인터페이스·오케스트레이션은 완성한다.

1. 패키지 레이아웃
   briefing/
     __init__.py
     __main__.py            # python -m briefing  (argparse: --date, --force, --dry-run)
     config.py              # .env + config/feeds.yaml 로드 (pydantic Settings)
     models.py              # 정규화 데이터 모델
     pipeline.py            # 오케스트레이션
     calendar_kr.py         # 영업일 판정
     logging_setup.py       # 파일+콘솔 로거, 키 마스킹 필터
     collectors/
       __init__.py
       krx.py               # 스텁: 지수 2개 + 투자자별 순매수 상위10
       dart.py              # 스텁: 공시 목록 + 카테고리 필터
       rss.py               # 스텁: 헤드라인
     analyze/
       __init__.py
       openai_client.py     # 스텁: 지금은 규칙기반 폴백 요약만
     render/
       __init__.py
       renderer.py
       template.md.j2       # PRD 7장 템플릿 그대로 (고지 문구 포함)
     deliver/
       __init__.py
       mailer.py            # 본문 생성까지만, 발송은 dry-run 로그
   config/feeds.yaml
   output/  data/  logs/    # 각각 .gitkeep
   tests/conftest.py, tests/fixtures/
   requirements.txt (핀 버전), .env.example (값 비움), .gitignore, README.md

2. models.py (pydantic)
   - IndexQuote: name, close, change, change_pct
   - InvestorFlowItem: name, code, net_buy_krw_eok, net_buy_qty|None
   - InvestorFlows: 외국인/기관/개인 × 코스피/코스닥 각 상위10 리스트
   - Disclosure: corp_name, stock_code, report_name, category(Enum: 실적/증자/감자/최대주주변경/공급계약),
     received_at(datetime, KST), url, key_figures(dict), is_correction(bool), time_parsed(bool)
   - NewsHeadline: title, url, source, published_at
   - BriefingPayload: target_date, window_start, window_end, kospi, kosdaq, flows, disclosures[], news[]
   - Pick: name, code, basis_type(Enum: disclosure/flow), basis_text, source_url|None
   - BriefingOutput: market_summary(str), picks(list[Pick]), generated_at

3. config.py
   - Settings: OPENAI_API_KEY, OPENAI_MODEL="gpt-4o", OPENDART_API_KEY, DATA_GO_KR_KEY|None,
     SMTP_HOST/PORT/USER/PASS, MAIL_FROM, MAIL_TO, BRIEFING_TZ="Asia/Seoul", NEWS_MAX_ITEMS=15
   - feeds.yaml 파서 -> [{name, url}], max_items

4. pipeline.run(target_date: date | None, force: bool, dry_run: bool) -> int
   - KST 기준 target_date(기본 오늘). 영업일 아니면 로그 남기고 return 0.
   - 수집 창 계산: 직전 영업일 18:00 ~ target_date 07:00 (KST, aware datetime).
   - output/브리핑_YYYY-MM-DD.md 이미 있으면(force 아니면) 스킵하고 return 0 (멱등).
   - krx / dart / rss 를 각각 try/except로 호출(하나 실패해도 계속). 성공·실패·건수를 로그.
   - 원본 스냅샷을 data/YYYY-MM-DD/{krx,dart,rss}.json 으로 저장.
   - analyze.generate(payload) 호출 (지금은 폴백: 수급 순매수 1~3위를 근거로 picks 구성, market_summary는 수치 나열).
   - renderer로 output/브리핑_YYYY-MM-DD.md 생성.
   - mailer.send(..., dry_run=True): 본문만 로그, 실제 발송 없음.
   - 종료코드: 치명 오류만 1, 부분 실패는 0.

5. calendar_kr.py
   - 주말 + 2026 공휴일 하드코딩 목록 + KRX 임시휴장 TODO 주석.
   - prev_business_day(d), is_business_day(d).

6. logging_setup.py: logs/run_YYYYMMDD.log + 콘솔. 정규식 마스킹 필터(키/토큰/비밀번호).

7. tests: 스텁 데이터로 pipeline.run 이 끝까지 돌아 .md 가 생성되는 스모크 테스트 1개.

산출물: 위 파일 전부 + `python -m briefing --date 2026-09-03 --dry-run` 실행 결과와 생성된 .md 내용.
```

---

## 2단계 — 기능 추가 (실제 수집 · 분석 · 발송)

목표: 1단계 스텁을 실제 구현으로 교체. 모델·인터페이스는 유지.

```
역할: 시니어 파이썬 개발자. PRD.md와 공통 컨텍스트를 따른다. 1단계 뼈대 위에 실제 구현을 채운다.

[2.1] collectors/krx.py
- data.krx.co.kr `getJsonData.cmd` (POST). Referer/User-Agent 헤더 설정. 타임아웃·재시도 3회(지수 백오프).
- 전일 코스피/코스닥 지수: 종가, 전일대비, 등락률.
- 투자자별(외국인/기관/개인) 순매수 상위 10: 코스피/코스닥 각각. 금액(억원), 가능하면 수량.
- bld 값은 파일 상단 상수 + 주석. 응답 파싱은 별도 함수로 분리(스키마 변동 대응).
- 실패 시: DATA_GO_KR_KEY 있으면 공공데이터포털 금융위 주식시세정보로 지수만 대체. 그래도 실패면 빈 결과 + 사유 로그.

[2.2] collectors/dart.py
- `list.json`: crtfc_key, bgn_de=직전영업일, end_de=target_date, page_count 최대, 페이지네이션 처리.
- 1차 필터: report_name 을 정규화(공백/특수문자 제거)해 PRD F2.3 표 항목과 매칭. `[정정]` 접두 허용(is_correction=True).
- 2차 필터: 각 건의 DART 상세에서 정확한 접수시각 파싱 -> 수집 창 밖이면 제외.
  상세 요청 간 0.5s 간격, 분당 한도 방어. 파싱 실패 시 rcept_dt 날짜로 보수적 포함 + time_parsed=False.
- key_figures 파싱(가능 범위): 공급계약=계약금액·최근매출액대비%, 증자=규모·방식, 감자=비율, 최대주주변경=전/후, 실적=매출·영업익·증감률. 실패해도 항목 유지.
- url: dart.fss.or.kr 뷰어 URL.

[2.3] collectors/rss.py
- feeds.yaml 매체별 피드 파싱(feedparser). 수집 창 내 발행분만.
- 저장: title, url, source, published_at 만. 본문/요약 필드는 버린다.
- 중복 제거: 제목 정규화 후 완전일치 + 간단 유사도. 시장/종목 키워드 포함 항목 우선 정렬. 최대 NEWS_MAX_ITEMS.

[2.4] analyze/openai_client.py
- openai SDK, 모델 OPENAI_MODEL, 호출 1회.
- 입력: payload 요약본(JSON) — 수급 상위10 + 필터된 공시(+key_figures) + 뉴스 제목만.
- system 프롬프트: PRD F4.4 제약 전부 명문화(추측 금지 / 매수·매도·보유·목표가 금지 / 제공 수치 외 사실 생성 금지 / 뉴스 본문 재작성 금지).
- 응답: JSON 스키마 강제(response_format json_schema). 필드: market_summary(한 줄), picks[<=3]{name, code, basis_type, basis_text(수치 포함), source_url?}.
- 후처리 검증: 각 pick 의 code 가 payload 내 실제 종목인지, basis_text 가 실제 공시/수급 수치를 인용하는지 대조. 불일치 pick 제거.
- 실패/스키마 위반: 규칙 기반 폴백(수급 1~3위 근거) + "AI 요약 생성 실패, 수치 기반 대체" 표기.
- 금지표현 후처리: 추천/매수/목표가/기대/전망 등 사전 매칭 시 해당 문장 제거 또는 1회 재요청.

[2.5] render/renderer.py
- 실제 데이터로 template.md.j2 채움. 빈 섹션은 "데이터 없음". picks 3개 미만이면 확보분만.

[2.6] deliver/mailer.py
- smtplib STARTTLS. 제목 `[마켓 브리핑] YYYY-MM-DD`. 본문: md->HTML + 원본 .md 첨부 + plain-text 대체 파트.
- 발송 실패는 예외 삼키고 로그. 파이프라인은 성공 처리(파일은 이미 저장됨).

[2.7] tests
- 각 수집기: 저장된 응답 fixture로 파싱 단위 테스트.
- DART 카테고리 매칭(정상/정정/비대상), 시각 경계(17:59 / 18:00 / 06:59 / 07:00).
- analyzer 폴백 경로, 뉴스 본문 미포함 검증.

산출물: 변경 파일, 테스트 결과, .env 실제 값으로 1회 실행한 브리핑 예시(민감정보 마스킹).
```

---

## 3단계 — UI 디자인 (브리핑 / 메일 비주얼)

목표: 출력물의 가독성·구조를 개선. **데이터 파이프라인 로직은 건드리지 않는다.**

```
역할: 이메일 HTML에 강한 프론트엔드/디자인 개발자. PRD.md와 공통 컨텍스트를 따른다.

1. HTML 이메일 템플릿 render/template.email.html.j2
   - 인라인 CSS만(이메일 클라이언트 호환), table 기반 레이아웃, 최대폭 640px, 모바일 반응형.
   - 라이트/다크 모두 읽히는 절제된 색, 시스템 폰트 스택. 외부 CSS/JS/폰트/이미지 CDN 금지.
   - 구조:
     헤더(날짜 · "오전 7시 기준")
     -> 시장 한 줄 요약 (강조 박스)
     -> 지수 표 (코스피/코스닥: 종가·전일대비·등락률, 상승/하락 색)
     -> 투자자별 순매수 (외국인/기관/개인 섹션, 각 코스피/코스닥 서브표, 상위10)
     -> 오늘 눈여겨볼 종목 3 (카드형: [공시]/[수급] 배지 + 근거 수치 + 원문 링크)
     -> 뉴스 (제목=링크 목록, 매체 배지, 발행시각)
     -> 푸터 (고지 문구, 출처, 생성 시각)
   - 접근성: table th scope, 링크 밑줄 유지, 본문 대비 4.5:1 이상.
   - 색상 토큰은 템플릿 상단 한 곳에서 관리.

2. plain-text 대체 본문 템플릿 정리(줄맞춤·구분선).

3. template.md.j2 도 동일 정보구조로 정돈: 헤딩 레벨, 표 정렬, 배지는 `[공시]`/`[수급]` 텍스트.

4. 로컬 아카이브 생성기: output/index.html
   - 과거 브리핑 목록(날짜 링크) + 최신 브리핑 미리보기. 정적 파일, 외부 리소스 없음.
   - pipeline 마지막 단계에서 갱신(로직 변경 아닌 호출 1줄 추가만).

5. mailer 가 HTML 파트로 새 템플릿을 사용하도록 연결.
   샘플 payload로 렌더한 결과를 tests/fixtures/sample_briefing.html 로 저장(시각 회귀용).

6. 라이트/다크/모바일 폭에서 렌더 확인 결과를 스크린샷 또는 설명으로 제시.
```

---

## 4단계 — PRD 준수 및 버그 검토

목표: 먼저 **진단 리포트**를 낸다. 승인 전 코드 수정 금지. 발견마다 근거(파일:라인), 재현/영향, 심각도(P0~P3), 수정 제안.

```
역할: 깐깐한 코드 리뷰어 + QA. PRD.md 를 기준으로 구현을 전수 점검한다. 먼저 리포트만 작성하고, 사용자 승인 후 P0/P1 수정을 적용한다.

[A] PRD 기능 대조 — F1~F6 각 항목을 표로: 구현됨 / 부분 / 누락 / 사양불일치 + 근거(파일:라인)
  - F1: 지수 3필드, 투자자 3구분 × 2시장 × 10종목, 실패 시 대체·"데이터 없음"
  - F2: 수집 창 정확성(타임존·경계), 카테고리 필터가 F2.3 표와 정확히 일치(누락/과다),
        정정공시 표기, 상세 파싱 실패 폴백, 원문 링크 유효
  - F3: 창 필터, 제목/링크/매체/발행시각만 저장, 본문 미저장, 중복 제거, 상한
  - F4: 호출 1회, 스키마 강제, 제약 프롬프트 존재, 후처리 검증(payload 대조), 폴백, 금지표현 필터
  - F5: 파일명/경로, 템플릿이 PRD 7장과 일치, 데이터 없음 처리, 고지 문구, 멱등
  - F6: 07:00 트리거 가이드(README), 영업일 판정, 재시도, 미가동 보정

[B] 버그 헌트
  - 타임존(naive/aware 혼용), 날짜 롤오버, 월요일(직전 영업일=금요일), 연휴 처리
  - Windows 인코딩(cp949 콘솔 vs 파일 UTF-8), 경로 구분자
  - 네트워크: 타임아웃 미설정, 무한/과도 재시도, DART 분당 한도 초과
  - 파싱: KRX/DART 응답 스키마 변동 시 크래시 vs graceful degrade
  - OpenAI: 스키마 위반, 토큰 초과, 빈 picks, 존재하지 않는 종목코드 인용, 근거 없는 pick
  - 부분 실패가 전체 실패로 전파되는 경로, 예외를 조용히 삼키는 지점
  - 멱등성: --force 동작, 메일 중복 발송 가능성

[C] 보안 / 컴플라이언스
  - .env 키가 로그/스냅샷/메일/예외/커밋에 노출되는지 (grep)
  - 뉴스 본문·요약이 어딘가에 저장/출력되는지 (grep)
  - 추측/매수추천 표현이 템플릿·폴백·프롬프트 예시에 남아있는지
  - .gitignore 커버리지(.env, output/, data/, logs/)

[D] 테스트 / 관측성
  - 경계 테스트 존재 여부, 픽스처 최신성, 로그만으로 실패 원인 추적 가능 여부

[E] 실행 검증
  - `python -m briefing --date <최근 영업일>` 실제 실행. 생성된 .md / .html / 로그 확인 후 결과 첨부.

산출물: 위 항목을 담은 마크다운 리포트(심각도순). P0/P1 은 수정 패치도 함께 제시.
필요하면 /code-review 스킬로 교차 점검.
```
