# GitHub Pages 배포 가이드

매 영업일 07:00 KST에 GitHub Actions가 브리핑을 생성하고, 결과 HTML을
`gh-pages` 브랜치에 누적 커밋하여 웹앱처럼 볼 수 있게 한다.

- 공개 페이지 주소(예): `https://<계정>.github.io/<저장소>/`
- 최신 브리핑 + 과거 목록: 위 주소의 `index.html`
- 특정 날짜: `.../브리핑_YYYY-MM-DD.html`

## 구성 요소 (이미 준비됨)

| 파일 | 역할 |
|---|---|
| `.github/workflows/pages.yml` | cron(월~금 22:00 UTC = 07:00 KST) + 수동 실행 → 파이프라인 → `gh-pages` 배포 |
| `briefing/config.py` | `BRIEFING_OUTPUT_DIR` 등 환경변수로 산출 경로 재정의 (CI가 `published/`로 지정) |
| `briefing/render/archive.py` | `index.html`(목록+미리보기) + `.nojekyll` 생성 |

## 처음 1회만 필요한 수동 단계

로컬은 이미 `git` 저장소로 초기화되어 있고 첫 커밋이 있다.

### 1) GitHub에 빈 저장소 생성
github.com → New repository → **Public**, 이름 예: `market-briefing`
(README/gitignore/license 체크 해제 = 빈 저장소)

### 2) 원격 연결 후 푸시
```bash
cd "C:/Users/KOCOwj/Desktop/브리핑"
git remote add origin https://github.com/<계정>/market-briefing.git
git branch -M main
git push -u origin main
```
(인증 창이 뜨면 GitHub 계정으로 로그인 / PAT 사용)

### 3) 저장소 Secrets 등록
저장소 → Settings → Secrets and variables → Actions → **New repository secret**

| 이름 | 값 |
|---|---|
| `OPENAI_API_KEY` | 새 OpenAI 키 (현재 `.env`의 키는 401 무효 상태) |
| `OPENDART_API_KEY` | `e957f5d0…` (현재 사용 중인 키) |
| `DATA_GO_KR_KEY` | (선택) 공공데이터포털 키 — KRX 지수 폴백용 |

`.env` 파일은 커밋되지 않는다(`.gitignore`). 키는 Secrets에만 둔다.

### 4) Pages 활성화
저장소 → Settings → Pages → **Source: Deploy from a branch** →
Branch: `gh-pages` / `/ (root)` → Save
(`gh-pages` 브랜치는 워크플로 첫 실행 후 자동 생성된다. 그 뒤 이 설정을 하면 됨)

### 5) 첫 실행
저장소 → Actions → "Daily briefing → GitHub Pages" → **Run workflow**
→ 완료되면 `gh-pages` 브랜치 생성 → 4)에서 Pages Source 지정 → 수 분 뒤 페이지 공개

## 참고 / 한계

- **KRX**: GitHub Actions 러너(해외 IP)에서도 KRX 정보데이터시스템이 차단될 수 있다.
  그 경우 지수·투자자별 수급은 "데이터 없음"으로 나오고 공시·뉴스는 정상 생성된다.
  `DATA_GO_KR_KEY`를 넣으면 지수만 공공데이터포털로 대체 시도한다.
- **이메일 발송은 CI에서 하지 않는다**(`--send` 미사용). Pages 게시만 한다.
  메일도 자동화하려면 워크플로에 SMTP Secrets + `--send` 추가.
- 과거 브리핑은 `gh-pages` 브랜치에 계속 쌓인다(약 30KB/일).
- 휴장일에는 파이프라인이 아무것도 생성하지 않으므로 페이지가 갱신되지 않는다.
- 공휴일 목록은 `briefing/calendar_kr.py`에 하드코딩되어 있으니 연 1회 갱신할 것.
