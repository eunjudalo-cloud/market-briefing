# 데일리 마켓 브리핑 자동화 — PRD

- 문서 버전: v0.2
- 작성일: 2026-09-04
- 상태: 주요 결정 확정 (11장 참조), 구현 착수 가능

---

## 1. 개요

### 1.1 배경
매 거래일 오전, 국내 증시 관련 핵심 정보(지수, 투자자 수급, 마감 후 공시, 뉴스 헤드라인)를
여러 사이트를 직접 돌며 취합하는 데 시간이 든다. 이를 자동화해 **매 영업일 오전 7시**에
정리된 브리핑을 만든다.

### 1.2 목적
- 오전 7시(KST) 기준, 전일 시장 데이터와 "장 마감 후 ~ 당일 새벽" 공시를 자동 수집·정리
- OpenAI 모델로 요약 및 "눈여겨볼 종목" 선별 — **근거는 반드시 수치(공시·수급) 기반**
- 정보 정리 목적. **투자 자문·매매 추천 아님** (추측·매수 추천 문구 금지)

### 1.3 범위
**포함**
- KRX/공공데이터 기반 전일 코스피·코스닥 지수
- 투자자별 순매수 상위 10종목
- OPENDART 공시 수집 + 카테고리 필터
- 경제지 RSS 헤드라인(제목·링크만)
- OpenAI 기반 요약/종목 선별
- 브리핑 산출물 생성 및 스케줄 실행

**제외 (이번 버전)**
- 해외 증시·선물·환율
- 종목 차트/기술적 지표
- 장중 실시간 업데이트
- 백테스트·수익률 추적

### 1.4 성공 기준
- 영업일 07:00 ± 5분 내 브리핑 파일 자동 생성
- 지수·수급·공시·뉴스 4개 섹션이 모두 채워지거나, 실패 시 해당 섹션만 "데이터 없음"으로 표기되고 나머지는 정상 생성
- "눈여겨볼 종목 3개"가 모두 공시 문구 또는 수급 수치를 근거로 포함
- 추측성·추천성 표현 0건 (프롬프트 제약 + 후처리 점검)

---

## 2. 용어
| 용어 | 정의 |
|---|---|
| 실행 시각 | 매 영업일 07:00 KST |
| 대상일 (T) | 브리핑 실행일 |
| 전일 (T-1) | 직전 영업일 |
| 공시 수집 창 | T-1 18:00 ~ T 07:00 (KST) |
| 영업일 | KRX 정규장 개장일 (주말·공휴일·임시휴장 제외) |

---

## 3. 사용자 / 사용 시나리오
- 주 사용자: 본인 (개인 투자·리서치 참고용)
- 시나리오: 07:00 자동 실행 → 브리핑 파일 생성(및 선택적 알림) → 출근 전 확인

---

## 4. 기능 요구사항

### F1. 시장 데이터 수집 (KRX / 공공데이터)
- **F1.1** 전일 코스피 지수: 종가, 전일대비 등락폭, 등락률
- **F1.2** 전일 코스닥 지수: 동일 항목
- **F1.3** 투자자별 순매수 상위 10종목
  - 투자자 구분: 외국인 / 기관 / 개인 (3구분 전부)
  - 시장 구분: 코스피 / 코스닥
  - 항목: 종목명, 종목코드, 순매수 금액(억원), 가능 시 순매수 수량
- **F1.4** 데이터 소스 우선순위
  1. KRX 정보데이터시스템 `getJsonData.cmd` (bld 지정, OTP 불요)
  2. 공공데이터포털 금융위 주식시세정보 API (지수/시세 보조·대체)
- **F1.5** 데이터 미제공(휴장·지연) 시 해당 섹션 "데이터 없음" 표기, 파이프라인 중단 금지

### F2. 공시 수집 (OPENDART)
- **F2.1** OPENDART `list.json`으로 T-1 ~ T 접수 공시 목록 조회
- **F2.2** 수집 창(T-1 18:00 ~ T 07:00)으로 시각 필터
  - `list.json`의 `rcept_dt`는 **날짜 단위**만 제공 → **DART 공시 상세 페이지를 파싱**해 정확한 접수시각(`YYYY-MM-DD HH:MM`)을 추출하고 이 값으로 수집 창 필터링
  - 절차: `bgn_de=T-1`, `end_de=T`로 목록 조회 → 카테고리 1차 필터 → 각 `rcept_no`의 상세(`dsaf001/main.do?rcpNo=...` 또는 뷰어 메타)에서 접수시각 파싱 → 창 밖 항목 제외
  - 상세 요청은 순차 처리 + 요청 간 간격(기본 0.5s)으로 부하 관리, 실패 시 해당 건은 `rcept_dt` 날짜만으로 보수적 포함(표기)
- **F2.3** 보고서명 기준 카테고리 필터 — **아래만 통과**
  | 분류 | 대상 보고서명(예) |
  |---|---|
  | 실적 | 영업(잠정)실적, 연결재무제표기준영업(잠정)실적, 매출액또는손익구조30%(대규모법인15%)이상변동 |
  | 증자 | 유상증자결정, 무상증자결정, 유무상증자결정 |
  | 감자 | 감자결정 |
  | 최대주주변경 | 최대주주변경, 최대주주변경을수반하는주식양수도계약체결 |
  | 단일판매·공급계약 | 단일판매ㆍ공급계약체결 |
- **F2.4** 공시 항목: 회사명, 종목코드, 보고서명, 접수시각, 원문 링크(`rcept_no` 기반), 핵심 수치(계약금액/증자규모/실적 등 파싱 가능 범위)
- **F2.5** 정정공시(`[정정]`)는 포함하되 별도 표기

### F3. 뉴스 헤드라인 수집 (RSS)
- **F3.1** 대상 매체 RSS — 설정 파일(`config/feeds.yaml`)로 관리
  - 초기 확정 목록: 한국경제, 매일경제, 서울경제, 이데일리, 연합뉴스(경제) — 각 매체 "증권/마켓" 피드 우선, 없으면 경제 전체 피드
  - 추가/제외는 `feeds.yaml` 수정만으로 반영
- **F3.2** 수집 창 내 발행 기사만
- **F3.3** 저장·출력 항목: **제목, 링크, 매체명, 발행시각** — 본문/요약 전문 저장·출력 **금지** (저작권)
- **F3.4** 제목 유사 중복 제거, 시장·종목 키워드 관련 항목 우선 정렬
- **F3.5** 최대 N건 (기본 15)

### F4. 분석 / 생성 (OpenAI)
- **F4.1** `.env`의 `OPENAI_API_KEY`, `OPENAI_MODEL`(기본 `gpt-4o`) 사용
  - 실행당 호출 1회 원칙(요약 + 종목 선별을 한 번에), 폴백 재시도 포함 최대 2회
  - 입력 토큰 절감: 수급 상위 10 + 필터된 공시 + 뉴스 제목만 전달 (원문 본문 미포함)
- **F4.2** 입력: F1~F3의 정규화된 JSON (수집 원본 그대로, 모델이 새 수치 생성 못 하도록 명시)
- **F4.3** 산출
  - **시장 한 줄 요약** — 코스피·코스닥 등락률 + 투자자 수급 방향을 수치로
  - **오늘 눈여겨볼 종목 3개** — 각 종목마다 근거를 **공시 문구 또는 수급 수치**로 명시
- **F4.4** 프롬프트 제약 (시스템 프롬프트에 명문화)
  - 추측성 표현 금지 ("~할 듯", "기대된다", "가능성" 등)
  - 매수/매도/보유 추천, 목표가·밸류에이션 판단 금지
  - 제공된 수치·공시 외 사실 생성 금지 (환각 방지)
  - 뉴스는 제목·링크만, 본문 재작성 금지
- **F4.5** 모델 응답은 JSON 스키마 강제 → 템플릿에 매핑
- **F4.6** OpenAI 호출 실패 시 규칙 기반 폴백 요약(수치 나열)으로 대체하고 브리핑은 생성

### F5. 브리핑 출력
- **F5.1** 포맷: Markdown, 파일명 `브리핑_YYYY-MM-DD.md`
- **F5.2** 저장 위치: 작업 폴더 하위 `output/`
- **F5.3** 템플릿: 7장 참조
- **F5.4** 전달 채널: **이메일** (확정)
  - 브리핑 Markdown을 본문(HTML 변환) + `.md` 첨부로 발송
  - SMTP 설정은 `.env`(`SMTP_HOST`/`SMTP_PORT`/`SMTP_USER`/`SMTP_PASS`/`MAIL_FROM`/`MAIL_TO`)
  - 발송 실패 시 파일은 이미 `output/`에 저장된 상태 유지, 로그에 실패 기록 후 다음 실행 시 재발송 안 함(당일 1회)

### F6. 스케줄링
- **F6.1** 매 영업일 07:00 KST 실행
- **F6.2** 실행 수단: **Windows 작업 스케줄러** (로컬 PC). 트리거 매일 07:00, 작업 = `python -m briefing`
  - PC가 07:00에 꺼져 있던 경우 "가장 최근 예약 시간 이후 가능한 한 빨리 실행" 옵션 사용
  - 이미 당일 브리핑 파일이 있으면 재실행 시 스킵(멱등)
- **F6.3** 영업일 판정: 주말·공휴일 제외 (KRX 휴장일 목록 또는 지수 데이터 유무로 판정)
- **F6.4** 실패 시 재시도 최대 3회(5분 간격), 최종 실패 시 알림

---

## 5. 비기능 요구사항
- **성능**: 전체 실행 5분 이내
- **안정성**: 한 소스 실패가 전체 실패로 이어지지 않음(부분 산출)
- **보안**: API 키는 `.env`에만, 저장소 커밋 금지(`.gitignore`), 로그에 키 미출력
- **컴플라이언스 / 저작권**
  - 뉴스 본문 저장·재배포 금지 — 제목 + 링크만
  - 브리핑 하단에 "정보 제공 목적, 투자 자문·매매 추천 아님" 고지 문구 고정
  - KRX 비공식 엔드포인트 사용 시 약관 확인 (대체: 공공데이터포털 API)
- **로깅**: 실행별 로그(`logs/run_YYYYMMDD.log`) — 수집 건수, 필터 결과, 오류 기록
- **재현성**: 수집 원본 JSON 스냅샷 저장(`data/YYYY-MM-DD/`)

---

## 6. 데이터 소스 상세

### 6.1 KRX 정보데이터시스템
- 엔드포인트: `http://data.krx.co.kr/comm/bldAttendant/getJsonData.cmd` (POST)
- 참고 bld (실사용 전 검증 필요)
  - 지수 일별시세: `dbms/MDC/STAT/standard/MDCSTAT00101` 계열
  - 투자자별 거래실적(종목): `dbms/MDC/STAT/standard/MDCSTAT024xx` 계열
- 파라미터: 조회일자, 시장구분, 투자자구분
- 인증: 불요 (단, `Referer`/`User-Agent` 헤더 필요할 수 있음)
- 대체: 공공데이터포털 서비스키 `DATA_GO_KR_KEY`

### 6.2 OPENDART
- 목록 API: `https://opendart.fss.or.kr/api/list.json`
- 파라미터: `crtfc_key`, `bgn_de`, `end_de`, `page_count`, `pblntf_ty`(공시유형), `corp_cls`
- 원문 링크: `https://dart.fss.or.kr/dsaf001/main.do?rcpNo={rcept_no}`
- 인증: `.env`의 `OPENDART_API_KEY`
- 제약: 분당 요청 한도, `rcept_dt` 날짜 단위

### 6.3 경제지 RSS
- 매체별 RSS URL 목록: `config/feeds.yaml`
- 파서: 표준 RSS/Atom

---

## 7. 출력 템플릿

```markdown
# 데일리 마켓 브리핑 — YYYY-MM-DD (오전 7시 기준)

## 시장 한 줄 요약
{한 줄. 코스피/코스닥 등락률과 투자자 수급 방향을 수치로.}

## 지수 (전일 종가)
| 지수 | 종가 | 전일대비 | 등락률 |
|---|---|---|---|
| 코스피 | ... | ... | ...% |
| 코스닥 | ... | ... | ...% |

## 투자자별 순매수 상위 10 (전일)
### 외국인 — 코스피 / 코스닥
1. 종목명(코드) +XXX억
...
### 기관 — ...
### 개인 — ...

## 오늘 눈여겨볼 종목 3
1. 종목명(코드)
   - 근거: (공시) 단일판매·공급계약 체결, 계약금액 XXX억(최근 매출액 대비 XX%), 접수 MM-DD HH:MM — {링크}
2. 종목명(코드)
   - 근거: (수급) 전일 외국인 순매수 1위 +XXX억, 기관 +XX억 동반
3. 종목명(코드)
   - 근거: (공시) 최대주주 변경, {요지} — {링크}

## 뉴스 헤드라인 (제목·링크)
- [매체] 제목 — https://...
...

---
본 자료는 정보 제공 목적이며 투자 자문이나 매매 추천이 아닙니다.
출처: KRX, OPENDART, 각 매체 RSS · 생성 YYYY-MM-DD 07:00 KST
```

---

## 8. 아키텍처 / 처리 흐름
1. 스케줄러가 `main` 실행 (07:00)
2. 영업일 판정 → 비영업일이면 로그만 남기고 종료
3. 병렬 수집: F1 / F2 / F3 → 각자 JSON 정규화, `data/`에 스냅샷 저장
4. 통합 페이로드 구성
5. F4: OpenAI 호출 (JSON 스키마 응답) → 실패 시 규칙 기반 폴백
6. F5: 템플릿 렌더링 → `output/브리핑_YYYY-MM-DD.md`
7. 이메일 발송 (본문 HTML + `.md` 첨부)
8. 로그 기록 / 종료 코드 반환

**권장 스택**: Python 3.11 · `requests` · `feedparser` · `openai` · `python-dotenv` · `pydantic`(스키마) · `jinja2`(템플릿)

---

## 9. 설정

### 9.1 `.env`
```
OPENAI_API_KEY=...
OPENAI_MODEL=gpt-4o
OPENDART_API_KEY=...
DATA_GO_KR_KEY=...            # 선택 (공공데이터포털 대체 경로)
BRIEFING_TZ=Asia/Seoul
BRIEFING_RUN_HOUR=7
NEWS_MAX_ITEMS=15

# 이메일 발송 (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=...
SMTP_PASS=...                 # 앱 비밀번호 권장
MAIL_FROM=...
MAIL_TO=eunjudalo@gmail.com
```

### 9.2 `config/feeds.yaml`
```yaml
feeds:
  - name: 한국경제
    url: https://...      # 증권 섹션 RSS
  - name: 매일경제
    url: https://...
  - name: 서울경제
    url: https://...
  - name: 이데일리
    url: https://...
  - name: 연합뉴스
    url: https://...      # 경제
max_items: 15
```

---

## 10. 예외 처리
| 상황 | 처리 |
|---|---|
| 휴장일 | 파이프라인 스킵, 로그만 |
| KRX 응답 실패 | 지수/수급 "데이터 없음", 나머지 진행 |
| OPENDART 한도/실패 | 재시도 3회, 실패 시 공시 섹션 부분/공란 |
| RSS 일부 실패 | 해당 매체만 스킵 |
| OpenAI 실패 | 규칙 기반 폴백 요약 |
| 눈여겨볼 종목 3개 미달 | 근거 확보된 수만 표기 (근거 없으면 제외) |
| 이메일 발송 실패 | `output/` 파일은 유지, 로그 기록, 당일 재발송 안 함 |
| 07:00 PC 미가동 | 부팅 후 즉시 실행, 당일 파일 있으면 스킵 |

---

## 11. 결정 사항 (확정)
| # | 항목 | 결정 |
|---|---|---|
| 1 | 공시 시각 필터 | **DART 상세 페이지 파싱**으로 접수시각 추출 후 수집 창 필터 (F2.2) |
| 2 | 전달 채널 | **이메일** (SMTP, 본문 HTML + `.md` 첨부) (F5.4) |
| 3 | 투자자 구분 | **외국인·기관·개인 3구분 전부** (F1.3) |
| 4 | RSS 매체 | 한국경제·매일경제·서울경제·이데일리·연합뉴스(경제), `feeds.yaml`로 관리, 최대 15건 |
| 5 | OpenAI 모델 | `gpt-4o` 고정(`OPENAI_MODEL`로 변경 가능), 실행당 호출 1회 원칙 |
| 6 | 실행 환경 | 로컬 **Windows 작업 스케줄러**, 07:00 미가동 시 부팅 후 즉시 실행, 멱등 스킵 |
| 7 | 언어·스택 | **Python 3.11** + requests / feedparser / openai / python-dotenv / pydantic / jinja2 |
| 8 | KRX 엔드포인트 | KRX 정보데이터시스템 우선, 실패 시 공공데이터포털 API 대체 (F1.4) |

### 남은 확인 (구현 중 조정 가능)
- 각 매체 RSS의 정확한 URL (증권 섹션 피드 우선) — 구현 시 확정해 `feeds.yaml`에 기재
- KRX 투자자별 거래실적 `bld` 값 — 실제 응답으로 검증 후 확정
- Gmail 사용 시 앱 비밀번호 발급 필요

---

## 12. 향후 확장
- 환율 / 미국 증시 / 야간선물
- 종목별 뉴스 자동 매핑
- 전일 대비 수급 연속성(며칠째 순매수 등) 분석
- 웹 대시보드 / 브리핑 아카이브
